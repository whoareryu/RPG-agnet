# CLAUDE.md

이 저장소는 **자율 캐릭터 멀티에이전트 자동대전 시스템**(Wanted AI Championship 2026)이다.
기획 원문은 `docs/기획/`, 설계는 `docs/superpowers/specs/`, 구현 계획은 `docs/superpowers/plans/`.
아키텍처와 코드 규약은 `~/Documents/secu-agent` 를 따른다.

## 1. 생각하고 나서 코딩한다

**가정하지 않는다. 혼란을 숨기지 않는다. 트레이드오프를 드러낸다.**

- 가정은 명시한다. 기획서에 답이 있으면 기획서가 이긴다 — 조항 번호를 주석에 적는다.
- 해석이 여럿이면 제시한다. 조용히 고르지 않는다.
- 더 단순한 방법이 있으면 말한다.

## 2. 단순함이 먼저다

- 요청 밖 기능은 만들지 않는다. C단계(비전) 기능은 **타입 선언까지만**.
- 한 번 쓰는 추상화는 만들지 않는다. 포트(Protocol)는 구현이 둘 이상일 때만 둔다.
- 200줄이 50줄이 될 수 있으면 다시 쓴다.

## 3. 수술적으로 고친다

- 바꿔야 하는 것만 만진다. 인접 코드·주석·포맷을 "개선"하지 않는다.
- 내 변경이 만든 고아(import·변수)만 치운다.

## 4. 목표 기반 실행 — 검증할 수 있는 성공 기준

- "규칙 추가" → "실패하는 테스트를 먼저 쓰고 통과시킨다" (TDD).
- 완료를 말하기 전에 검증 명령을 실제로 돌리고 출력을 본다.

---

## 이 프로젝트의 규칙

### 계층 (backend/)
```
core/       안쪽. 표준 라이브러리만. adapters·api·eval·content 를 모른다
adapters/   바깥. core 를 안다. LLM 모델 ID 는 adapters/llm/ 에만
api/        FastAPI — 얇은 HTTP 계층. 도메인 판단 없음
eval/       실험 러너
content/    세계관 데이터(로스터·클래스·몬스터·환경) — 바깥 계층
```
`tests/test_boundaries.py` 가 AST 로 강제한다.

### 기획서가 테스트로 굳혀진 것
- **성별은 엔진 영향 0** (기획서 §4.4) — `core/rules`·`core/battle`·`core/judgment` 는 `gender` 를 읽지 않는다.
- **행운은 판단에 개입하지 않는다** (§4.2) — `core/judgment` 는 `luck` 을 읽지 않는다.
- **MBTI 는 표기일 뿐** (§4.5) — 프롬프트에 MBTI 문자열이 없다.
- **유저 포인트는 뺏지 않는다** (§5) — `Stats` 에 감소 경로가 없다.
- **같은 시드 = 같은 판** — 난수는 전부 `Dice` 포트를 통한다. `random` 직접 호출 금지.

### 수치 단일 출처
밸런스 수치(주사위 식·페널티 계수·승산 임계·마스킹 단계·포인트 총량)는 `core/rules/constants.py` 한 곳에 있다. 다른 곳에 리터럴을 쓰지 않는다. 설계 문서 13장이 근거다.

### 코드 규약
- 테스트 함수 이름은 한국어 (`test_지혜가_낮으면_아군_상태가_가려진다`).
- 주석은 "무엇"이 아니라 "왜". 기획서 조항을 인용한다.
- 커밋 메시지는 한국어 평서형(`~했다`). 제목 한 줄 + 본문에 이유.
- 파이썬 명령은 `backend/` 에서 `uv run …`. 프론트는 `frontend/` 에서 `npm …`.
- 도메인 용어: 단주(유저) · 단장(오케스트레이터) · 단원(캐릭터 에이전트) · 인스펙터 · 트레이스.

### 검증 명령
```bash
cd backend && uv run ruff check . && uv run ruff format --check . && uv run pytest -q
cd frontend && npm test && npx tsc --noEmit
```

### 페르소나 QA
마일스톤이 끝날 때마다 `.claude/agents/qa-*.md` 다섯 페르소나로 리뷰를 돌린다 (`/qa-personas`).
발견은 `docs/qa/` 에 남기고, 고치고, 재검한다. 발견을 무시하고 넘어가지 않는다.
