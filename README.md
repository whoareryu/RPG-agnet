# 에르덴 용병단

**캐릭터를 조종하는 게임이 아니라, 고용하는 게임.**
자율 캐릭터 멀티에이전트 자동대전 — 게임을 검증 환경(testbed)으로 쓴다.

> Wanted AI Championship 2026 출품작 · 기획서는 [`docs/기획/`](docs/기획/)

---

## 무엇이 다른가

에이전트 데모는 흔하다. **에이전트가 제멋대로 움직이는데 왜 그랬는지 전부 설명되는 것**은 흔하지 않다.

당신은 변경의 용병단 단주다. 단원 다섯 중 셋을 골라 폐광의 군주에게 보낸다. 그다음부터 손을 뗀다.
전장에서는 단장 AI 가 작전을 짜고, 단원 AI 가 매 턴 판단한다 — 때로는 방침을 어기고, 때로는 도망친다.

```
행동: 카일이 전장을 이탈했다
├ 입력된 상황: HP 33%, 아군 이탈 1, 부양가족 1
├ 이탈 압력: HP 0.335 + 아군 이탈 0.1 + 생애 0.2 = 0.635
├ 성향 보정: 위험 +0.09, 희생 +0.12, 협동 -0.04  → +0.17
├ 순응 판정: 이탈 확률 0.95, 주사위 0.13 → 이탈
└ 사유: "여기서 죽을 수는 없다. 두 살 딸이 집에 있다."
```

화면에서 그 행동을 클릭하면 이 기록이 열린다. 플레이어는 단원의 속마음을 못 보지만, 우리는 전부 안다.
**그래픽은 없다.** 대사와 흐름과 판정값뿐이다. 이건 못 만든 게 아니라 안 만든 것이다 — 이 프로젝트의
정체는 게임이 아니라 **에이전트 트레이스 뷰어**다.

## 세 장면

| 장면 | 무엇이 보이는가 |
|---|---|
| **이탈** | 두 살 딸이 있는 궁수가 방침을 버린다. 이탈 확률·성향 보정·주사위가 전부 남는다 |
| **포기** | 단장이 "어떻게 이기는가" 이전에 "싸울 가치가 있는가"를 판단하고 사람을 데리고 나온다 |
| **읽힘** | 보스가 같은 자의 반복 공격·마법 편중·반복된 치유·방어 일변도를 관측하고 대응한다 |

## 돌려보기

자격증명이 없어도 게임 전체가 돈다 — 기본 모델이 규칙 기반 Fake 다. 판단이 LLM 보다 단순할 뿐
규칙·로그·인스펙터는 동일하다.

```bash
# 백엔드
cd backend && uv sync --dev
uv run uvicorn api.main:create_app --factory --port 8000

# 화면 (다른 터미널)
cd frontend && npm ci
BACKEND_URL=http://localhost:8000 npm run dev     # http://localhost:3000
```

또는 한 번에:

```bash
docker compose up --build
```

## 구조

```
backend/
  core/          안쪽. 표준 라이브러리만 — 프레임워크도 콘텐츠도 모른다
    types.py       엔티티 · 값 객체
    ports.py       경계 Protocol (안쪽이 소유, 바깥이 구현)
    rules/         주사위 · 신체 · 능력치 · 효율 스펙트럼 · 판정 — 순수 함수
    battle/        진영 대칭 전투 상태 머신
    judgment/      지혜 마스킹 · 승산 · 순응 판정 · 재계획 트리거
    agents/        단장 · 단원 · 보스 — 포트를 통해 모델을 부른다
    trace/         트레이스 스키마 v1
    runner.py      미션 N개 + 인터미션. N 은 파라미터다
  adapters/      바깥. core 를 안다
    llm/           fake · replay · (gemini · anthropic)
    harness/       JSON 스키마 강제 · 검증 · 재시도 · 폴백
    store/         JSONL 런 기록
  content/       세계관 — 로스터 · 클래스 · 몬스터 · 환경 · 미션
  api/           FastAPI — 얇은 HTTP 계층
  eval/          실험 러너 E1·E2·E3
frontend/        Next.js — 진입 · 로스터 · 2패널 뷰어 · 결과 · 실험
```

**의존성은 항상 안쪽을 향한다.** `backend/tests/test_boundaries.py` 가 AST 로 강제하고,
기획서의 약속 세 가지도 같은 방식으로 굳혀 두었다:

| 기획서 조항 | 테스트 |
|---|---|
| 성별은 전투·능력치·판단에 영향 0 | `core` 에서 `gender` 를 읽는 파일은 서사 표기 하나뿐 |
| 행운은 굴림에만, 판단에 미개입 | `luck` 을 읽는 파일은 `core/rules/combat.py` 하나뿐 |
| MBTI 는 표기일 뿐 | 조립된 프롬프트에 MBTI 문자열이 없다 |
| 같은 시드는 같은 판 | 난수는 전부 `Dice` 포트를 통한다. CI 가 **다른 프로세스**에서도 확인한다 |

## 실험 — 말이 아니라 숫자로

```bash
cd backend && uv run python -m eval.run -e e1 -n 30
```

**E1 · 단장이 실제로 도움이 되는가** (조합당 시드 30, Fake 모델)

| 조합 | 단장 ON 승률 | OFF 승률 | ON 생존 | OFF 생존 |
|---|---:|---:|---:|---:|
| 균등 | 93% | 83% | 99% | 92% |
| 마법사 몰빵 | 63% | 57% | 86% | 68% |
| 힐러 없음 | 60% | 80% | 72% | 91% |
| 전사 몰빵 | 13% | 33% | 71% | 76% |

**두 조합에서 단장이 승률을 깎는다.** 단장이 후퇴를 명령하기 때문이고, 후퇴한 24판의 사망자가
단장 없이 싸운 쪽과 같은 21명이었다 — 아무도 더 살리지 못하면서 판만 버렸다. 이것은 숨기지 않는다.
실험의 목적이 "감독이 도움이 되나"를 재는 것이고, 지금 답은 **"조합에 따라 다르다"** 이다.
Fake 단장의 판단 품질 문제이지 기구의 고장이 아니라는 근거는 [QA 기록](docs/qa/)에 있다.

**E3 · 성향이 실제로 행동을 가르는가** (수준당 시드 20, 다른 축은 0 고정)

| 희생 수용도 | 이탈 / 판 | 도망 비율 | 승률 |
|---:|---:|---:|---:|
| -80 | 16.3 | 2% | 60% |
| 0 | 6.2 | 1% | 90% |
| +80 | 2.4 | 0% | 95% |

다양성이 말이 아니라 **검증된 판단 분기**라는 뜻이다.

## 검증

```bash
cd backend  && uv run ruff check . && uv run ruff format --check . && uv run pytest -q
cd frontend && npm test && npx tsc --noEmit
```

파이썬이 만든 `docs/trace-samples/one-run.jsonl` 을 프론트 테스트가 읽는다 — 양쪽이 같은 파일을
읽으므로 트레이스 계약이 갈라질 수 없다.

## 문서

- [기획서 v2](docs/기획/기획서_v2_2026-09-06.md) · [팀원용 쉬운 설명서](docs/기획/팀원용_쉬운설명서.md)
- [설계 문서](docs/superpowers/specs/2026-09-07-rpg-autonomous-party-design.md) — 기획서의 미확정 수치를 확정한 곳
- [구현 계획](docs/superpowers/plans/2026-09-07-rpg-a-and-b-stage.md)
- [기획서 조항 ↔ 코드 대조표](docs/spec-trace.md) — 무엇이 구현됐고 무엇이 남았는지
- [QA 기록](docs/qa/) — 페르소나 다섯이 찾은 것과 고친 것
