"""테스트 전역 설정.

`RPG_PACE_S` 를 0 으로 못박는다. 배포 데모의 기본 박자(`select.FAKE_PACE_S`)는
뿔피리를 누를 시간을 만들려고 0.2 인데, 그대로 두면 테스트 전체가 판단 하나당
0.2초씩 잔다 — 실측 7초 → 78초. 테스트는 박자를 재지 않는다.

박자 자체를 재는 테스트(`test_Fake_는_기본으로_박자를_받는다`)는 이 변수를
직접 지웠다 되돌린다.
"""

import os

os.environ.setdefault("RPG_PACE_S", "0")
