---
date: 2026-09-07
area: adapter
seq: 10
---

- **JSONL 저장소 · Replay 모델 · 모델 선택 · 트레이스 샘플을 넣었다**
  Replay 는 녹화된 plan/decision 의 raw 응답을 역할별로 순서대로 돌려준다 —
  같은 시드면 규칙 엔진이 같은 질문을 같은 순서로 하므로 모델 호출 0 으로 같은
  판이 재생된다(기획서 §11.3 데모 빨리감기). docs/trace-samples/one-run.jsonl
  은 시드 8 가렛/일레인/카일 판으로 이탈 9회·보스 적응·재계획·포기가 한 판에
  들어 있다. 파이썬 테스트와 프론트 테스트가 같은 파일을 읽어 계약을 지킨다.

- 커밋 `3327526` · 파일 8개
