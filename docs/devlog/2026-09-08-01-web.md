---
date: 2026-09-08
area: web
seq: 1
---

- **QA 라운드 1 의 P0 다섯 · P1 여덟을 고쳤다**
  P0-1 재계획 트리거: 이탈·적응 신호를 읽기 전에 비우고 있었다. 160판 계측에서
  replan_trigger 가 전부 odds_collapse 였다 — 기획서 §8.3 의 트리거 4종 중 둘이
  죽어 있었다. begin_turn/consume_signals 로 나눴다.

  P0-2 결정론: latency_ms 가 payload 에 실려 "같은 시드 = 같은 트레이스" 가 매번
  깨졌다. 시간 메타를 timing 으로 분리하고, 결정론 비교의 정본을
  core/trace/schema.judgment_view 하나로 통일했다 — 테스트마다 다르게 정의하고
  있었고 한쪽은 항상 빨갛고 한쪽은 통과했다.

  P0-3 프로세스 간 결정론: 보스 적응이 set 순회로 대상을 골라 PYTHONHASHSEED 에
  따라 판이 갈렸다. 등장 순서로 돌고 tie_rule 을 기록한다.

  P0-4 전사가 공격하지 않았다: Fake 가 스킬 목록의 첫 번째를 골랐고 전사 순서가
  (방패 밀치기, 강타) 라 가렛이 40판 동안 총 피해 0 이었다. 프롬프트 컨텍스트에
  스킬이 무엇을 하는지(offensive/effect)를 넣고 Fake 가 그것을 읽는다.

  P0-5 사망이 소멸이 아니었다: 1판에서 죽은 단원이 2판에 만피로 섰다(기획서 §6.6).

  P1: 후퇴는 명령이지 선택지가 아니다(모델을 부르지 않는다) · 이탈은 라벨이 아니라
  선택지의 변화다 · 출전 1~3명(§7.2) · 턴 상한은 턴 종료에서만 본다 · 정수 포인트
  검증 · 행운이 도적 무기 선택에 개입하던 것 제거(§4.2) · 보스 적응이 실제로 바꾼
  것을 effect 로 남긴다 · 폴백 응답도 스키마 검증 · 임계 범위 단일 출처.

  승산 휴리스틱을 time-to-kill 로 바꾸고 순응 기댓값을 반영했다. HP 비율만 보던
  식은 HP 148 보스와 HP 60 수하를 같은 무게로 셌고, 전사 파티가 "승산 0.66" 을
  들으며 지는 판이 나왔다. 감독의 편성도 고쳤다 — 근접 적은 전열이 살아 있는 한
  후열에 닿지 못하는데 전사를 전부 앞에 세우고 있었다.

  QA 라운드: docs/qa/2026-09-07-m4-one-run.md

- 커밋 `21341b5` · 파일 62개
