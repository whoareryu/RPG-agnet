---
date: 2026-09-08
area: infra
seq: 3
---

- **배포 준비를 갖췄다 — Dockerfile · compose · CI · README**
  자격증명 없이 뜬다. 기본 모델이 Fake 라 방문자는 API 키 없이 한 판을 완주한다
  (기획서 §9 추가①). 런 기록은 볼륨에 둬 재시작해도 완료된 판을 다시 연다.

  CI 에 프로세스 간 결정론 검사를 넣었다. 한 프로세스 안에서만 재는 테스트는
  해시 순서 의존을 못 잡고, 실제로 그 결함이 승패까지 갈랐다(QA 라운드 1 P0-3).
  PYTHONHASHSEED 를 달리한 두 프로세스가 같은 판정 뷰를 내는지 diff 한다.

  트레이스 샘플은 측정 시간을 0 으로 눕혀 재생성이 바이트 안정적이다 —
  그러지 않으면 다시 만들 때마다 git diff 가 소음으로 찼다.

  README 에 E1 결과를 그대로 적었다. 두 조합에서 단장이 승률을 깎는다는 사실을
  숨기지 않는다 — 실험의 목적이 그것을 재는 것이다.

- 커밋 `28f5e48` · 파일 11개
