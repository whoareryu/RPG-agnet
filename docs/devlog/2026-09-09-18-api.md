---
date: 2026-09-09
area: api
seq: 18
---

- **공허하던 테스트와 틀린 지표를 고쳤다 — 재검 P1 R11~R20**
  R11: 18출동 회귀 테스트가 버그를 되돌려도 초록이었다. 쓰던 편성이
  11판째 전멸해 합계 293 호출로 끝나 옛 상한 300 에 닿지도 못했다.
  18판을 완주하는 편성으로 바꾸고 예산 자체를 직접 단언한다. 돌연변이로
  빨개지는 것을 확인했다.

  R12: _focus_follow 가 effect.field 를 안 봐서 summon_faster 의 정수
  after(2)를 "지목" 으로 읽었다. 방패병 몰빵 지목 명중 23% vs 참값 47%.
  차트와 커밋된 e2.json 이 그 틀린 숫자를 싣고 있었다.

  R13: eval 이 build_harness 를 우회해 "예산은 계약 길이만큼" 이 정작
  그 숫자를 재는 곳에서만 안 먹었다. 실험은 박자 0 으로 돈다.

  R14: 뿔피리 HTTP 테스트 둘이 100% 공허했다(첫 요청 200 이 20회 중
  0회). 박자를 주는 클라이언트로 왕복을 실제로 지나게 했고, turn<=1
  버그를 되살리면 빨개진다.

  R15: content/party.py 가 voice 때문에 유스케이스를 import 하고
  있었고 경계 테스트에 content 규칙이 아예 없었다. voice 의 죽은 성별
  분기(두 갈래가 같은 문자열)를 지우고 content 로 옮겼다. 규칙 추가.

  그 밖에 뿔피리 턴의 승산(유저가 보던 값), 계약 샘플의 horn 과 미덮인
  kind 명시, 출정 요청 오타 거절, stat_max 단일 출처, paired 의 축
  혼동, avg_calls 의 잘못된 이름, 「섭식」이 걸 데 없을 때의 정직한 표시.

  QA 라운드: docs/qa/2026-09-09-v3-rework.md

- 커밋 `20e68b7` · 파일 26개
