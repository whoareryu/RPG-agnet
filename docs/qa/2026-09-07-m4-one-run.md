# QA 라운드 1 — 마일스톤 1~4: 규칙 엔진 · 전투 · 판단 · 러너 한 판 완주

- 검사 대상 커밋: `3327526` (브랜치 stage-a)
- 범위: `backend/core/**`, `backend/adapters/{harness,llm,store}/**`, `backend/content/**`, `backend/tests/**`, `docs/trace-samples/one-run.jsonl`
- 아직 없는 것: API(마일스톤 5) · 프론트(6) · 평가 러너(7) · 인터미션(8). 이것들의 부재는 발견이 아니다.
- 검증 명령: `cd backend && uv run ruff check . && uv run pytest -q` → 276 passed

## 발견
